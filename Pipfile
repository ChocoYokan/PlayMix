[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[scripts]
start = "pipenv run python manage.py runserver"
makemigrate = "pipenv run python manage.py makemigrate"
migrate = "pipenv run python manage.py migrate"

[packages]
django = "*"
djangorestframework = "*"

[dev-packages]

[requires]
python_version = "3.9"
